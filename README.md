# mytodo
A to-do webapp structured and managed by YEOMAN

# How to use the app
clone the repository
make sure you have latest Node installed and node package manager
open the project and start node server with <npm run serve> from the terminal within the project directory

# implements;
yeoman scaffolding tool
